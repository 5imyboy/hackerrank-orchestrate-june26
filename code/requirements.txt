anthropic>=0.49.0
pydantic>=2.6.0
python-dotenv>=1.0.0
# Image handling: dataset files are often mislabeled (.jpg that are really WebP
# or AVIF). Pillow + the AVIF plugin let us detect the real format and convert
# AVIF (unsupported by the API) to PNG before sending.
pillow>=10.0.0
pillow-avif-plugin>=1.4.0
